# Done

Tasks that passed all verification gates. Newest at top.

Each entry: id, title, completion date, a summary (a sentence for trivial
tasks, a paragraph when the detail earns its keep — the journal holds the
full story), the delivery record (``PR #N, merged `sha` `` under the `pr`
profile), and a journal pointer.

- TASK-NNN — title — YYYY-MM-DD — summary — PR #N, merged `sha`. See journal YYYY-MM-DD [HH:MM]

## Completed

- TASK-014 — Dashboard parser → model — 2026-07-21 — `parseDashboard` reads a single-file Active/Backlog/Done dashboard into `[]Card` (ADR-0004): Active/Done are pipe tables (columns mapped by header name; cells split on unescaped `|`, `\|` a literal pipe), Backlog is bullet groups (a `**Group:**` line labels its bullets). Title = leading `**bold**` (subtitle after the em-dash); every inline `[A-Z]+-\d+` → `Card.Tickets` (0..N, first-seen; `#NNN` PR refs excluded); Material/Record link + Status cell (unescaped) captured; `blocked` = a leading `⛔`/`**Blocked` marker. Defensive: an empty-title row → a `dashboard-malformed` /_diag warning + skip, never a panic. `Card` grew optional Dashboard/Subtitle/Tickets/Group/Material/Status/Blocked fields (zero for framework cards). Verified: `TestParseDashboard` on a rich fixture (3/4/2 cards, all fields, escaped pipes, groups, multi/zero-ticket, malformed row) + `TestSplitDashTitle`; `-race` clean; real Pinata KB smoke → In Progress 4 / Backlog 12 / Done 16 = 32 cards, `/_diag` "No warnings", `/_v` live. Fresh-context review PASS 5/5; fixed a [low] subtitle-split bug (em-dash inside the bold title) + a doc nit; ticket false-positives (`UTF-8`-style) acknowledged per-contract, tightening parked. — PR #29, merged `2697702`. See journal 2026-07-21 15:44.
- TASK-013 — Dashboard-mode resolution — 2026-07-21 — `Resolve` now recognizes the single-file dashboard adapter (ADR-0004): manifest lookup tries `README.md` then `index.md`; a `dashboard:` Locations key or the new `--dashboard FILE` flag selects dashboard mode and lifts the planning-dir requirement (value resolved repo-root-relative, like other Locations paths); `Areas.DashboardFile` drives a parser dispatch (`defaultParser`) that returns an empty board for now (parsing is TASK-014), and `areaMTime` watches the single file so `/_v` stays live. Framework + multi-system modes unchanged. Verified: unit tests for both resolution paths + missing-file/non-dir errors; smoke on the real Pinata KB (`--dashboard knowledge/work/index.md` → `dashboard: …/work/index.md`, `/`→200) and the Locations-key fixture (`/`→200, `/_v` a real mtime); `bklg .` framework line unchanged. Fresh-context review PASS all 5 AC, zero correctness findings; the one coverage nit (missing-file via Locations) fixed with a test. — PR #27, merged `6cea51f`. See journal 2026-07-21 15:20.
- TASK-012 — Multi-system board (aggregate + filter) — 2026-07-20 — At a monorepo root manifest, `main` now builds a multi-server (was error+exit): `Server.board()` resolves+parses every `systems/<name>` instance, tags each card with its `System` (chip on board + detail), and concatenates; a server-side `?system=` filter bar lists every discovered system (incl. empty); detail `/{id}` is a global lookup; `/_v` aggregates mtimes; an unresolvable system is skipped with a `/_diag` warning. Single-system mode unchanged. ADR-0003. On real `trail`: `aggregate: 5 systems`, ~105 cards, `?system=track`→15 no-leak, `/TRACK-000`→200. Fresh-context review PASS; fixed a [low] single-mode `?system=` no-op contract bug. — PR #24, merged `95acdf7`. See journal 2026-07-20 17:52.
- TASK-011 — Make /_diag actionable — 2026-07-20 — Structured warnings (`Warning{Kind,Message,TaskRaw}`); `/_diag` is now an HTML page (via the layout) grouping warnings by kind with a count, a what-it-means/how-to-fix explanation, and links from id-bearing warnings to their detail page; "shipped item missing from DONE.md" reframed as informational (expected when records live inline on the `[x]` line). On trail: 14 shipped-missing warnings collapse to one grouped, linked, explained section. Fresh-context review PASS (escaping verified); fixed a latent map-order nondeterminism in the unknown-kind fallback. — PR #22, merged `ce95127`. See journal 2026-07-20 17:30.
- TASK-010 — Render a safe markdown subset — 2026-07-20 — `markdown.go` `renderMarkdown`: escape-first (`html.EscapeString`) then a whitelist of inline patterns (`**bold**`/`*italic*`/`` `code` ``/`[t](url)`) → `template.HTML`; link hrefs scheme-checked (`safeURL` rejects `javascript:`/`data:`/protocol-relative/backslash); code spans lifted so they aren't reprocessed; unmatched markers stay literal; asterisk-only emphasis (no `snake_case`/`__dunder__` mangling); NUL stripped. Templates render free-text fields via `{{md}}`; Raw block stays auto-escaped. Zero new deps (ADR-0002). Adversarial security review: **PASS, no injection** (~90 hostile inputs, structural fuzzing, `-race`). — PR #20, merged `ff2ea83`. See journal 2026-07-20 17:11.
- TASK-009 — Parser robustness for real instances — 2026-07-20 — Extended the line-scanner (ADR-0001) to parse `### <ID> — <title>` heading-style DONE entries (Date/PR/Journal from the `**Completed:** … · **PR:** … · **Journal:** …` line + prose Summary) alongside the skeleton bullet format, and made `parseID` strip leading markdown decoration (`* ` `` ` `` `_`) so `**WI-8 — …**` is recognized. On the real trail repo: TRACK-000's DONE entry now parses (its `/_diag` warning gone, detail shows Date/PR/Journal), and WI-8/9/10 parking items get linkable ids. The other 14 "missing from DONE.md" warnings are correct (trail keeps those in the `[x]` line) — TASK-011. Fresh-context review found + I fixed a latent `parseIDTitle` off-by-N for decorated-id-without-separator. — PR #18, merged `d0e33a5`. See journal 2026-07-20 16:47.
- TASK-008 — Trim card text — 2026-07-20 — Added a rune-safe `truncate` template helper (word-boundary backoff + ellipsis, ~140 runes) used for the board card title only; the detail page keeps the full title. Display-only. Verified on the real `trail` repo: 18 wall-of-text cards now trim to ~140 chars + `…`. Fresh-context review passed all 4 AC, no findings (index-safety on invalid UTF-8, escaping intact). — PR #16, merged `08c4053`. See journal 2026-07-20 16:30.
- TASK-007 — Live reload — 2026-07-20 — `areaMTime` computes the max mtime across CURRENT/BACKLOG/DONE/blockers (sets `Board.Meta.LatestMTime`); `GET /_v` returns it as a bare integer (text/plain, statted not reparsed). Every page bakes the same value and embeds ~8 lines of vanilla JS polling `/_v` every ~3s → `location.reload()` on change. Fresh-context review passed all 5 AC with **zero findings** (`-race` clean, XSS probe on the baked version, non-parsed-file edits correctly ignored, empty instance → `0`). This completes the v1 MVP. — PR #13, merged `14b1df6`. See journal 2026-07-20 15:35.
- TASK-006 — Task detail + 404 — 2026-07-20 — `GET /{id}` (case-insensitive, D2) → per-task detail: id/title/namespace/column/badges, then state-appropriate fields (In-Progress: source, acceptance checklist, notes, override; Done: date, summary, delivery, journal pointer), referencing blockers (open first, then resolved), and a collapsed Raw source block; unknown id / id-less card → 404; literal routes still win. Reinstated `Board.CardByRawID`; added `task.html` + `taskTmpl`. Fresh-context review passed all 5 AC with **zero findings** (injected `<script>` into all 16 fields → all escaped; path traversal → no file read/panic); one stale comment fixed. — PR #11, merged `cd00f36`. See journal 2026-07-20 15:24.
- TASK-005 — Board template + badges + Tailwind — 2026-07-20 — Embedded `html/template` (layout + board) + `render.go`: view model → Backlog/In Progress/Done columns, badge→Tailwind colour map (blocked red, parking slate, override amber, no-ac yellow, namespace gray), planning-path header + `/_diag` banner; `handleBoard` renders buffered. Repo text auto-escaped (no injection); Tailwind Play CDN; muted parking + empty-column placeholders. Fresh-context review passed all 5 AC with **zero findings** (incl. a full injection probe). Dogfood: `bklg .` renders this repo's own 7-card board. — PR #9, merged `a9a1fc3`. See journal 2026-07-20 15:11.
- TASK-004 — Blocker parse + blocked join — 2026-07-20 — `parseBlockers` reads `blockers.md`, disambiguating `BLOCKER-\d+` headings from Format/Open/Resolved sections (skipping Format); `computeBadges` joins planning+progress into all badges — `blocked` (open blocker's affected id, case-insensitive + trailing-text tolerant), `parking`, `override`, `no-ac`, `namespace`. New `internal/backlog/server.go`: per-request board build + `GET /_diag` (warnings verbatim, text/plain). Fresh-context review passed all 5 AC; its [low] finding (em-dash in blocker title) + a join note were fixed and pinned with tests. Dogfood `bklg . /_diag` → "no warnings". — PR #7, merged `835363f`. See journal 2026-07-20 14:57.
- TASK-003 — Planning parser → model — 2026-07-20 — `internal/backlog` model (§4) + a line-oriented `Parser` (interface per D4) turning CURRENT/BACKLOG/DONE into a `Board` of deduped `Card`s (most-advanced-state: Done > InProgress > Backlog) with the three §5 reconciliation warnings; DONE lines split so an em-dash inside the summary round-trips; reads degrade to warnings, never panics. Ships the rich `testdata/knowledge` demo instance. Verified against a quoted 7-card / 3-warning dump. Fresh-context review passed all 5 AC; its two [low] dedup edge-case findings (parking-with-id, shipped-in-CURRENT detail) were fixed + pinned with tests, fixture board unchanged. — PR #5, merged `42ff5c9`. See journal 2026-07-20 14:42.
- TASK-002 — Area resolution — 2026-07-20 — `internal/backlog.Resolve`: dereferences the manifest's `## Locations` block (repo-root-relative), falls back to `base/planning`+`base/progress`, and detects a multi-system root manifest (`systems/<name>` index) to list per-system invocations rather than erroring blankly; `main` echoes resolved paths and exits non-zero with a clear message (no server) on any failure. 8 unit tests + 5 `testdata/resolve` fixtures. Fresh-context review passed all 5 AC, no findings; two notes addressed (documented key matching, added a partial-block test). Dogfood: `bklg .` resolves this repo via its real Locations block. — PR #3, merged `4cf04c1`. See journal 2026-07-20 14:19.
- TASK-001 — CLI + server skeleton — 2026-07-20 — `splitArgs`-based CLI (positional in any position; `--flag v` and `--flag=v`), flags `--port`/`--dir`, loopback-only bind with clean port-in-use failure, and a `GET /{$}` handler returning a 200 HTML placeholder; startup block matches spec §9 verbatim. Fresh-context review passed all 5 AC; its two non-blocking notes (silent extra-positional drop, `-h` exit code) were fixed and covered by table-driven `splitArgs`/`joinDisplay` tests. — PR #1, merged `288a814`. See journal 2026-07-20 14:00.
